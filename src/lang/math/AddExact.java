package lang.math;

/**
 * Math.addExact(int a, int b) - Возвращает сумму своих аргументов, вызывая исключение,
 * если результат переполняет a int.
 */
public class AddExact {
    public static void main(String[] args) {
        System.out.println(Math.addExact(100,5));
    }
}
